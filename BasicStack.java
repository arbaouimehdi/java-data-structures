public class BasicStack<X> {
  private X [] data;

  public BasicStack(){
    // Initalize a data structure
    // to a new object array of 1000
    // it means hlding 1000 items in our stack
    data = (X[]) new Object[1000];
  }

}
